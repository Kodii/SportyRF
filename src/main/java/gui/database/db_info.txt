CATEGORY(   ID INTEGER PRIMARY KEY,
            NAME VARCHAR(50))

CONTEST(    ID INTEGER PRIMARY KEY,
            NAME VARCHAR(50),
            PLACE VARCHAR(50),
            DATE VARCHAR(50),
            LAPS VARCHAR(50),
            START_TYPE TINYINT)

EVENT(      CONTEST_ID INTEGER PRIMARY KEY,
            PARTICIPANT_ID SMALLINT,
            TEAM_ID SMALLINT,
            PAIR_ID SMALLINT)

PARTICIPANT(NAME VARCHAR(50),
            SURNAME VARCHAR(50),
            GENDER SMALLINT,
            BIRT VARCHAR(50),
            ID INTEGER PRIMARY KEY,
            CITY VARCHAR(50),
            DISTRICT VARCHAR(50),
            TEAM SMALLINT,
            WEIGHT VARCHAR(50),
            HEIGHT VARCHAR(50),
            CATEGORY SMALLINT,
            STARTNUMBER INTEGER)

START_TYPE( ID INTEGER PRIMARY KEY,
            NAME VARCHAR(50))

TEAMS(      ID INTEGER PRIMARY KEY,
            NAME VARCHAR(50))
            
GENDER(		ID INTEGER PRIMARY KEY,
			GENDER VARCHAR(50))